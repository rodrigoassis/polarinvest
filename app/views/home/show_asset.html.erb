<%= form_for :home, :url => { :action => :show_asset } do |f| %>

  <div><%= f.autocomplete_field :name, autocomplete_asset_name_dashboard_assets_path %></div>

  <div class="actions">
    <%= f.submit 'Buscar' %>
  </div>
<% end %>

<%= render partial: 'graphic' %>