<%= form_for :home, :url => { :action => :show_asset } do |f| %>

  <div><%= f.label :name %><br />
  <%= f.autocomplete_field :name, autocomplete_asset_name_assets_path %></div>

  <div class="actions">
    <%= f.submit 'Search' %>
  </div>
<% end %>

<%= render partial: 'graphic' %>