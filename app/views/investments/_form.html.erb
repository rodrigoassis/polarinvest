<%= form_for(@investment) do |f| %>
  <% if @investment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@investment.errors.count, "error") %> prohibited this investment from being saved:</h2>

      <ul>
      <% @investment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div><%= f.label :asset %><br />
  <%= f.select :asset_id, Asset.all.map{|asset| [asset.name, asset.id]} %></div>

  <div><%= f.label :type %><br />
  <%= f.select :type, [['InvestmentTypes::Saving'], ['Saving']] %></div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
